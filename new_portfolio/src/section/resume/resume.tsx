import { motion } from "framer-motion";

const Resume = () => {
  return (
    <motion.div 
      id="resume"
      className="flex flex-col min-h-screen bg-gradient-to-b from-[#002e4e] via-[#002945] to-[#001a2c]"  
    >
        
    </motion.div>
  );
};

export default Resume;
